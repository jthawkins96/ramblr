import './ChatList.scss';
import ChatPreview from './ChatPreview/ChatPreview';

const ChatList = () => {
    return (
        <div>
            <ChatPreview />
        </div>
    );
};

export default ChatList;
